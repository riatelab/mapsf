#' Deprecated functions in mapsf
#'
#' These functions still work but will be removed (defunct) in the next
#' major version of the package.
#'
#' Instead of using the following functions, one can use `mf_map()` with the
#' corresponding type:
#' - `mf_base() => mf_map(x, type  = "base")`
#' - `mf_choro() => mf_map(x, var, type = "choro")`
#' - `mf_prop() => mf_map(x, var, type = "prop")`
#' - `mf_typo() => mf_map(x, var, type = "typo")`
#' - `mf_symb() => mf_map(x, var, type = "symb")`
#' - `mf_grad() => mf_map(x, var, type = "grad")`
#' - `mf_prop_typo() => mf_map(x, var, type = "prop_typo")`
#' - `mf_prop_choro() => mf_map(x, var, type = "prop_choro")`
#' - `mf_symb_choro() => mf_map(x, var, type = "symb_choro")`
#' - `mf_init() => mf_map(x, type = "base", col = NA, border = NA)`
#'
#'
#' @md
#' @name mapsf-deprecated
NULL
